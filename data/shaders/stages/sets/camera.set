#softdefine CAMERA_DESCRIPTOR_SET_INDEX

layout(set = CAMERA_DESCRIPTOR_SET_INDEX, binding = 0) uniform CameraUbo {
    mat4 viewTransform;
    mat4 projectionTransform;
    vec4 wPosition;
    uvec2 extent;
} camera;

#include "../helpers/camera.sfunc"
