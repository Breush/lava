#softdefine GBUFFER_MAX_NODE_DEPTH

#define GBUFFER_MATERIALDATA_SIZE 10

layout(std430, set = 0, binding = 0) buffer GBufferHeader {
    uint width;
    uint listIndices[];
} gBufferHeader;

struct GBufferNode {
    uint materialId6_next26;
    float depth;
    uint materialData[GBUFFER_MATERIALDATA_SIZE];
};

layout(std430, set = 0, binding = 1) buffer GBufferList {
    uint counter;
    GBufferNode nodes[];
} gBufferList;
