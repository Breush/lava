#pragma once

namespace lava::chamber::math {
    constexpr const float PI = 3.14159265358979323846f;
    constexpr const float PI_OVER_TWO = PI / 2.f;
    constexpr const float PI_OVER_FOUR = PI / 4.f;
    constexpr const float TWO_PI = 2.f * PI;

    /// exp(EXP_HALF_LIFE) = 0.5
    constexpr const float EXP_HALF_LIFE = -0.693147f;
}
