#pragma once

#include <lava/core/macros.hpp>

$enum_class(lava, MouseWheel,
            Unknown,    // Unknown
            Vertical,   // Vertical mouse wheel (main)
            Horizontal, // Horizontal mouse wheel (secondary)
);
